import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6F4E37", // Coffee Brown
    },
    secondary: {
      main: "#D2B48C", // Latte Color
    },
  },
  typography: {
    fontFamily: "Poppins, sans-serif",
  },
});

export default theme;
